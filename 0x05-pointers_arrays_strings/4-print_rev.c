#include "main.h"

/**
 *print_rev - prints string in reverse
 *@s: string to print
 */

void print_rev(char *s)
{
	int h, l;
	char *k;

	h = strlen(s);
	*k[h] = " ";
	for (l = 0; l <= h; l++)
	{
		k[l] = s[h];
	}
	puts(k);
}
