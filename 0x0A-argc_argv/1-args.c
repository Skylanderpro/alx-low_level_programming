#include <stdio.h>
/**
 *main - prints the number of arguments to main
 *@argc: number of arguments to main
 *@argv: string storing arguments to main
 *Return: 0
 */
int main(int argc, char *argv[])
{
	argv = argv;

	printf("%d\n", argc - 1);

	return (0);
}
