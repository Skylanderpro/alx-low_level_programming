#include "main.h"
/**
 *main - prints it's name
 *@argc: number of arguments to main
 *@argv: array of arguments to main in commandline
 *Return: always 0
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
